import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        height: 64,
        alignItems: 'center',
        padding: 8,
        flexDirection: 'row',
    },
    backButtonContainer: {
        height: 48,
        width: 48,
        justifyContent: 'center',
        alignItems: "center"
    },
    backArrowIcon:{
        height: 24,
         width: 24 
    }
})