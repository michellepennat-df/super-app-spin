import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        height: 64,
        alignItems: 'center',
        padding: 8,
        paddingStart: 16,
        flexDirection: 'row',
    },
    backButtonContainer: {
        height: 24,
        paddingEnd: 10,
        paddingStart:8,
        justifyContent: 'center',
        alignItems: "center",
    },
    backArrowIcon: {
        height: 24,
        width: 24
    }
})