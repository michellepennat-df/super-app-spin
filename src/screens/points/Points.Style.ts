import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container:{
        flex:1,
        padding: 16,
    },
    card: {
        height: 80,
        width: '90%',
        justifyContent: 'center',
        flexDirection: 'row',
        alignItems: 'center',
        borderRadius: 8,
      },
    icon:{

    }
});
